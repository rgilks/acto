import type { Metadata, Viewport } from 'next';
import { Poppins } from 'next/font/google';
import './globals.css';
import AuthProvider from '@/components/AuthProvider';
import ClientPWAWrapper from '@/components/ClientPWAWrapper';

const poppins = Poppins({
  weight: ['400', '500', '600', '700'],
  subsets: ['latin'],
  variable: '--font-poppins',
  display: 'swap',
});

export const viewport: Viewport = {
  themeColor: '#000000',
};

export const metadata: Metadata = {
  title: 'acto - Interactive Adventure Game',
  description: 'Embark on thrilling interactive adventures powered by AI.',
  keywords: 'interactive adventure, AI game, storytelling, RPG, game',
  authors: [{ name: 'acto Team' }],
  metadataBase: new URL('https://acto.app'),
  openGraph: {
    title: 'acto - AI-Powered Interactive Adventures',
    description: 'Play captivating interactive adventure games generated by AI.',
    locale: 'en_US',
    type: 'website',
  },
  manifest: '/manifest.json',
  appleWebApp: {
    capable: true,
    statusBarStyle: 'default',
    title: 'acto',
  },
  twitter: {
    card: 'summary_large_image',
    title: 'acto',
    description: 'Experience interactive adventures brought to life by AI.',
  },
  icons: {
    icon: '/favicon.ico',
    apple: '/apple-touch-icon.png',
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <head>
        <link rel="icon" href="/favicon.ico" />
        <link rel="icon" href="/icons/icon-192x192.png" />
        <link rel="apple-touch-icon" href="/icons/icon-192x192.png" />
      </head>
      <body className={`${poppins.variable} font-sans`}>
        <AuthProvider>
          {children}
          <ClientPWAWrapper />
        </AuthProvider>
      </body>
    </html>
  );
}
